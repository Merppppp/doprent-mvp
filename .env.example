# Copy to .env.local for local dev.
# Both vars are optional — if missing the app falls back to data/sample-listings.json
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Default LINE OA used by the homepage / fallback CTAs (when a dress has no override)
NEXT_PUBLIC_DEFAULT_LINE_URL=https://line.me/R/ti/p/@doprent
